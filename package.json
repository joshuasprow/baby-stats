{
  "name": "baby-stats",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "client",
    "firebase",
    "functions",
    "lib",
    "models",
    "scripts"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  }
}
