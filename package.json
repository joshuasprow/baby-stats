{
  "name": "baby-stats",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "client",
    "firebase",
    "functions",
    "lib",
    "migrations",
    "models"
  ],
  "scripts": {
    "build": "turbo run --filter=baby-stats-client build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  }
}
