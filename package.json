{
  "name": "baby-stats",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "client",
    "firebase",
    "models",
    "scripts",
    "tsconfig"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "scripts": "turbo run --prefix scripts start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "npm@8.19.2"
}
