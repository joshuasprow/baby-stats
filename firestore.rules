rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow create, read, update: request.auth.uid != null && request.auth.uid == userId;
      match /days/{dayId} {
        allow create, read, update, delete: request.auth.uid == userId;
      }
    }
  }
}